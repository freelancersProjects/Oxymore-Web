{
  "cleanUrls": true,
  "buildCommand": "cd ../../ && npm run build && cd apps/oxymore-app && npm run build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
